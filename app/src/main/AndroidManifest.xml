<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vsokoltsov.uprogress">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".common.BaseApplication">
        <activity android:name=".authentication.ui.AuthorizationActivity">
        </activity>
        <activity android:name=".directions_list.ui.DirectionsActivity">
        </activity>
        <activity
            android:name=".directions_list.ui.DirectionDetailActivity"
            android:parentActivityName=".directions_list.ui.DirectionsActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.vsokoltsov.uprogress.directions_list.ui.DirectionsActivity" />
        </activity>

        <activity
            android:name=".launch.LaunchActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>